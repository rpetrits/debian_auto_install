# ==========================================
# 1. SPRACHE, TASTATUR & ZEITZONE
# ==========================================
d-i debian-installer/locale string de_DE.UTF-8
d-i keyboard-configuration/xkb-keymap select de

d-i clock-setup/utc boolean true
d-i time/zone string Europe/Vienna
d-i clock-setup/ntp boolean true

# ==========================================
# 2. NETZWERK & MIRROR (Globaler Server)
# ==========================================
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian-server
d-i netcfg/get_domain string local

d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# ==========================================
# 3. BENUTZER & SUDO-RECHTE
# ==========================================
# Root-Konto sperren, damit der erste User automatisch Sudo-Rechte erhält
d-i passwd/root-login boolean false

# Admin-Benutzer anlegen (Passwort: start123)
d-i passwd/user-fullname string Default User
d-i passwd/username string default
d-i passwd/user-password password start123
d-i passwd/user-password-again password start123

# ==========================================
# 4. PARTITIONIERUNG (Löscht die erste SATA-SSD)
# ==========================================
d-i partman-auto/disk string /dev/sda
d-i partman-auto/method string regular
d-i partman-lzma/supported_architectures string amd64
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# ==========================================
# 5. SOFTWARE-AUSWAHL & PAKETE
# ==========================================
d-i apt-setup/non-free-firmware boolean true
d-i apt-setup/cdrom/set-first boolean false

# Nur Standard-Werkzeuge und SSH-Server installieren (KEINE GUI)
d-i tasksel/first multiselect standard, ssh-server

# ZUSÄTZLICH: Sudo-Paket installieren (wichtig für den Admin-User)
d-i pkgsel/include string sudo

# ==========================================
# 6. BOOTLOADER & ABSCHLUSS
# ==========================================
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean false
d-i grub-installer/bootdev string default
d-i grub-installer/force-efi-extra-removable boolean true

d-i finish-install/reboot_in_progress note
